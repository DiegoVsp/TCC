<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MelhorAmigo.Paginas.Pag.Telefones"
             Title="TELEFONES">
    <ContentPage.Content>

        <ListView x:Name="ListaTelefones" HasUnevenRows="True" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Margin="20">
                            <StackLayout Orientation="Vertical" Margin="10">
                                <Label Text="{Binding Nome}" Margin="0" FontAttributes="Bold" FontSize="20" TextColor="#d72234"/>
                                <Label Text="{Binding Phone}" Margin="0" FontSize="15" TextColor="Black"/>
                            </StackLayout>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

    </ContentPage.Content>
</ContentPage>